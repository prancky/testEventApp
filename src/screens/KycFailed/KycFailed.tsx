import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { sharedStyles } from '@/components/shared/sharedStyles';

/**
 * AUTO-GENERATED placeholder for the extension navigation target "KycFailed", which is not
 * a designed screen. Overwritten if a real screen of this name is added. Keeps the
 * extension flow navigable in the Expo (designer) preview.
 */
const KycFailed = (): JSX.Element => {
  return (
    <View style={sharedStyles.container2}>
      <Text style={sharedStyles.title}>KycFailed</Text>
      <Text style={sharedStyles.subtitle}>Extension result placeholder</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default KycFailed;
